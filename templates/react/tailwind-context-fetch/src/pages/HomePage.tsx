// Layouts
import MaxWidthWrapper from "@/layout/wrapper/MaxWidthWrapper";

export default function HomePage() {
  return (
    <main>
      <MaxWidthWrapper className="flex items-center justify-center w-screen h-screen">
        <h1>Start by editing files in pages folder</h1>
      </MaxWidthWrapper>
    </main>
  );
}
